# Suggestions for AGENTS.md

- Add CLI prototype structure under "Project Structure" (e.g., `cli/` and `cli/tests/`).
- Add test guidance for Node's built-in test runner: `node --test cli/tests/*.test.js`.
- Document new artifact files (`n.codes.config.json`, `n.codes.capabilities.yaml`, `.n.codes.cache.json`) under "Security & Configuration Tips".
- Note that the capability map prototype writes JSON content in a `.yaml` file (valid YAML 1.2).
- Mention `npx n.codes` command expectations in the "Build, Test, and Development" section.
