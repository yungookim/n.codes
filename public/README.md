# n.codes Landing Page

## Local Development

### Prerequisites

Install `live-server` globally:

```bash
npm install -g live-server
```

### Running Locally

From this directory:

```bash
live-server
```

This will open the landing page in your default browser at `http://127.0.0.1:8080` with live reload enabled.
